name: PreferencesTest
version: '${version}'
main: dev.mintychochip.preferences.test.TestPlugin
api-version: '26.2'
depend: [Preferences]
description: Test hooking plugin that registers preferences via the public PreferencesService API.
